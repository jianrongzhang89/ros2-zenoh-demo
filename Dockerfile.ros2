FROM ros:jazzy-ros-base

RUN apt-get update && apt-get install -y \
    ros-jazzy-rmw-zenoh-cpp \
    ros-jazzy-demo-nodes-cpp \
    && rm -rf /var/lib/apt/lists/*

COPY zenoh-client.json5 /zenoh-client.json5
