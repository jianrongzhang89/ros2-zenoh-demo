apiVersion: apps/v1
kind: Deployment
metadata:
  name: gazebo-sim
  namespace: ros2-zenoh-gazebo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gazebo-sim
  template:
    metadata:
      labels:
        app: gazebo-sim
    spec:
      securityContext:
        runAsNonRoot: true

      # Tolerate the cluster-specific GPU node taints so the scheduler
      # can place this pod on g5.2xlarge (A10G) or g6e.2xlarge (L40S) nodes.
      tolerations:
        - key: g5-gpu
          operator: Exists
          effect: NoSchedule
        - key: g6e-gpu
          operator: Exists
          effect: NoSchedule

      initContainers:
        - name: wait-for-router
          image: quay.io/jianrzha/ros2-zenoh-gazebo:latest
          command:
            - python3
            - -c
            - |
              import socket, time, sys
              print("Waiting for zenoh-router:7447 ...")
              while True:
                  try:
                      s = socket.create_connection(("zenoh-router", 7447), timeout=2)
                      s.close()
                      print("Router is up.")
                      sys.exit(0)
                  except OSError:
                      time.sleep(1)
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]

      containers:
        # ── Container 1: Gazebo Harmonic simulation server + WebSocket bridge ─
        # gz-launch runs as a background process in this container so it
        # inherits the vendor LD_LIBRARY_PATH set by setup.bash, putting it
        # in the same gz-transport "universe" as gz sim.  Running it in a
        # separate container causes a cross-library-instance discovery failure.
        - name: gazebo-sim
          image: quay.io/jianrzha/ros2-zenoh-gazebo:latest
          command:
            - bash
            - -c
            - |
              source /opt/ros/jazzy/setup.bash
              # Start WebSocket server after gz sim is ready (15 s warm-up).
              # Runs in the background so gz sim remains PID 1.
              (sleep 15 && \
               /usr/lib/x86_64-linux-gnu/gz/launch7/gz-launch -v 4 \
               /etc/gz-ws/websocket.gzlaunch) &
              exec gz sim -v 4 -s -r --headless-rendering /etc/gz-sim/diff_drive.sdf
          env:
            # GPU-accelerated EGL rendering via NVIDIA A10G/L40S.
            # OGRE2 uses hardware EGL — no display server needed.
            # This enables the simulated lidar sensor to produce /scan data.
            - name: DISPLAY
              value: ""
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: graphics,compute,utility
            # Expose all available GPUs — overrides the device plugin's per-container
            # NVIDIA_VISIBLE_DEVICES=void default, ensuring OGRE2 can find the GPU.
            - name: NVIDIA_VISIBLE_DEVICES
              value: all
            - name: ROS_HOME
              value: /tmp
          ports:
            - containerPort: 9002
              protocol: TCP
          volumeMounts:
            - name: robot-model
              mountPath: /etc/gz-sim
            - name: gz-websocket-config
              mountPath: /etc/gz-ws
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
              nvidia.com/gpu: "1"
            limits:
              cpu: "2"
              memory: 4Gi
              nvidia.com/gpu: "1"

        # ── Container 2: ros_gz_bridge — gz-transport ↔ ROS 2 DDS ───────────
        # Runs in the same pod network namespace as gazebo-sim, so
        # gz-transport (ZeroMQ) discovery stays on localhost automatically.
        - name: ros-gz-bridge
          image: quay.io/jianrzha/ros2-zenoh-gazebo:latest
          command:
            - bash
            - -c
            - |
              source /opt/ros/jazzy/setup.bash
              exec ros2 run ros_gz_bridge parameter_bridge \
                --ros-args -p config_file:=/etc/gz-bridge/bridge.yaml
          env:
            # Restrict DDS discovery to localhost — the zenoh-bridge sidecar
            # in this same pod will forward traffic to the remote Zenoh router.
            - name: ROS_AUTOMATIC_DISCOVERY_RANGE
              value: LOCALHOST
            - name: ROS_HOME
              value: /tmp
            # CycloneDDS matches zenoh-bridge-ros2dds internal DDS, fixing
            # the "Bad Parameter" DDS writer error seen with FastRTPS in Jazzy.
            - name: RMW_IMPLEMENTATION
              value: rmw_cyclonedds_cpp
          volumeMounts:
            - name: gz-bridge-config
              mountPath: /etc/gz-bridge
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi

        # ── Container 3: zenoh-bridge-ros2dds — ROS 2 DDS ↔ Zenoh router ────
        # Discovers ROS 2 topics published by ros-gz-bridge on localhost DDS
        # and forwards them over Zenoh TCP to the central zenoh-router pod.
        - name: zenoh-bridge
          image: docker.io/eclipse/zenoh-bridge-ros2dds:latest
          args: ["-c", "/etc/zenoh/bridge.json5"]
          env:
            - name: ROS_AUTOMATIC_DISCOVERY_RANGE
              value: LOCALHOST
          volumeMounts:
            - name: zenoh-bridge-config
              mountPath: /etc/zenoh
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi

      volumes:
        - name: robot-model
          configMap:
            name: robot-model
        - name: gz-bridge-config
          configMap:
            name: gz-bridge-config
        - name: zenoh-bridge-config
          configMap:
            name: zenoh-bridge-config
        - name: gz-websocket-config
          configMap:
            name: gz-websocket-config
